# ===========================================
# Fourth Note Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# For Docker: copy to project root (~/fourth-note/.env)
# For Local:  copy to backend/.env

# -------------------------------------------
# Database Configuration
# -------------------------------------------
# Default credentials work out-of-box for Docker (DB not exposed externally)
# For local dev, create this user/database in your PostgreSQL
POSTGRES_USER=pitchdeck
POSTGRES_PASSWORD=fN4thN0te_db_2025!
POSTGRES_DB=pitchdeck

# DATABASE_URL format:
# - Docker:  postgresql://pitchdeck:PASSWORD@postgres:5432/pitchdeck
# - Local:   postgresql://pitchdeck:PASSWORD@localhost:5432/pitchdeck
DATABASE_URL=postgresql://pitchdeck:fN4thN0te_db_2025!@postgres:5432/pitchdeck

# -------------------------------------------
# Google APIs (Required)
# -------------------------------------------
GOOGLE_API_KEY=your_gemini_api_key_here

# -------------------------------------------
# Gmail Configuration
# -------------------------------------------
# Unix timestamp - fetch emails after this date
# 1769126400 = Jan 22, 2026
# 1735689600 = Jan 1, 2025
# 1704067200 = Jan 1, 2024
GMAIL_QUERY_SINCE=1769126400

# -------------------------------------------
# Scheduler Configuration
# -------------------------------------------
# How often to check for new emails (in hours)
# Set to 0 to disable automatic fetching
SCHEDULER_INTERVAL_HOURS=6

# -------------------------------------------
# Path Configuration (Local Development Only)
# -------------------------------------------
# These are only needed for local development without Docker
# Docker uses default paths via volume mounts
#
# DATA_DIR=./data
# TOKEN_FILE=./data/token.json
# CREDENTIALS_FILE=../credentials.json
