# ===========================================
# Fourth Note - Environment Configuration
# ===========================================
# Copy this file to .env in the project root
#
# For local Windows dev: docker compose -f docker-compose.dev.yml up
# For production:        docker compose up

# -------------------------------------------
# Database (Docker PostgreSQL)
# -------------------------------------------
POSTGRES_USER=pitchdeck
POSTGRES_PASSWORD=fN4thN0te_db_2025!
POSTGRES_DB=pitchdeck
DATABASE_URL=postgresql://pitchdeck:fN4thN0te_db_2025!@postgres:5432/pitchdeck

# -------------------------------------------
# Google APIs
# -------------------------------------------
GOOGLE_API_KEY=your_gemini_api_key_here

# Google OAuth (for user authentication)
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret

# -------------------------------------------
# JWT Authentication
# -------------------------------------------
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=change-me-in-production

# -------------------------------------------
# Gmail Configuration
# -------------------------------------------
# Unix timestamp - only fetch emails after this date
# 1769126400 = Jan 22, 2026
# 1735689600 = Jan 1, 2025
GMAIL_QUERY_SINCE=1769126400

# -------------------------------------------
# Scheduler
# -------------------------------------------
# Hours between automatic email fetches (0 to disable)
SCHEDULER_INTERVAL_HOURS=6

# -------------------------------------------
# Optional: AI Agent APIs (for development)
# -------------------------------------------
# OpenAI API key (for testing alternative models)
# OPENAI_API_KEY=your_openai_api_key

# Perplexity API key (for LinkedIn profile lookup agent - currently disabled)
# PERPLEXITY_API_KEY=your_perplexity_api_key
